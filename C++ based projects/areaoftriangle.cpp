#include <iostream>
using namespace std;

int main(){
    float a,b,h;
    cout << "Enter the Base and Height: ";
    cin >> b >> h;
    a = (b*h)/2;
    cout << "Area is: " << a << '\n';
    return 0;
}